{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": " webpack",
    "buildAndStart": "cd ../v-gen-lib && npm run build && cd ../v-cam-lib && npm run build && cd ../server && webpack && node server.js",
    "start": "node server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "express": "^4.21.2",
    "serve-index": "^1.9.1",
    "webpack": "^5.97.1",
    "webpack-cli": "^6.0.1",
    "worker-loader": "^3.0.8"
  },
  "dependencies": {
    "dotenv": "^16.5.0"
  }
}
